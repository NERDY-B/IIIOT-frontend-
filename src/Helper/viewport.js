import React from 'react'

export const defaultViewPort = (x, y) =>{
    window.scroll
}

//call this function 
// function checks the current position of the window,
// if the current window is greater than initial value, 
// set the curren window postion back to initial

//getting and setting  the value of the window first 
// const x = window.innerWidth;
// const y = window.innerHeight;

//if window.innerHeight > x{
    defaultViewPort(x,y)
// }